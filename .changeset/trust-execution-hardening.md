---
"thesmos-governance": minor
---

Trust Execution Hardening (phases 0–8): fail-closed assurance, safe Claude defaults, Pantheon `--execute`, builder/`agent:run` repairs, receipts + local metrics, release pins, health/catalog integrity, and score honesty via real `governance.log.jsonl` from `review`/`validate`/`ci`/MCP `scan_file`.
